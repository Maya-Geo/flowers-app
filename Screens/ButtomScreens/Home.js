import React from "react";
import { View, StyleSheet } from "react-native";
import BottomNavigator from "../../NavigatorRoute/BottomNavigator";
const Home = () => {
  return <BottomNavigator />;
};

const styles = StyleSheet.create({});

export default Home;
